{
  "name": "micathon-26",
  "version": "0.1.0",
  "private": true,
  "description": "Voice-Activated Vernacular Ledger (Digital Khata) — Micathon '26",
  "scripts": {
    "dev": "pnpm --filter client dev",
    "build": "pnpm --filter client build",
    "typecheck": "pnpm -r typecheck",
    "lint": "pnpm -r lint"
  },
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0"
  }
}
